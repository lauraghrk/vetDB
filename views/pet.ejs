<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>vetDB</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
  </head>
  <body>
    <h1 class="text-center mb-5">Vet DB</h1>
    <h2>Pet's information</h2>
    <div class="list-group">

      <div class="list-group-item list-group-item-light d-flex justify-content-between align-items-center">
        <div>
          <h5>Pet name</h5>
          <p><%= pet.petName %></p>
        </div>
        <a href="/edit/<%= pet._id %>" class="btn btn-dark">Edit</a>
      </div>
      
      <p><%= pet.species %></p>
      <p><%= pet.breed %></p>
      <p><%= pet.color %></p>
      <p><%= pet.birth.split('-').reverse().join('/') %></p>
      <p><%= pet.ownerName %></p>
      <p><%= pet.phone %></p>
      <p><%= pet.email %></p>
      <p><%= pet.address %></p>
    </div>

    <form action="/delete/<%= pet._id %>" method="post">
      <button type="submit" class="btn btn-danger btn-lg mt-5 mb-5">Delete register</button>
    </form>

    <a href="/" class="btn btn-secondary btn-lg">Cancel</a>
  </body>
</html>